// Create a folder `config` in root directory
// Rename this file to `config.js`, and include it with defined properties of configuration 

var configuration = {
  CLIENT_ID: '',
  CLIENT_SECRET: '',
  SESSION_SECRET: ''
}

module.exports = configuration;